# Арт-дирекшен «Наш 1 „Г“» — кинематографичная эволюция

Концепция редизайна приложения родительского комитета. Цель — вау-эффект и доверие 27 семей: премиум как форма заботы. Стиль — эволюция текущего тёплого языка до уровня «уютного кино», без потери простоты. Одна светлая тема, телефоны в приоритете.

---

## 1. Большая идея: «Золотой час нашего класса»

Всё приложение живёт в одном кадре: **кабинет 166 в лучах вечернего солнца**. Тот самый свет «золотого часа», в котором Pixar снимает самые тёплые сцены. Родитель открывает приложение — и попадает не в таблицу с деньгами, а в маленький тёплый мир своего класса, где всё разложено по местам и обо всём позаботились.

Формула настроения: *«Спокойствие родителя, у которого всё под контролем»*. Каждое решение — палитра, свет, движение — проверяем вопросом: «Стало ли спокойнее и теплее?» Если эффект кричит о себе — убираем.

## 2. Айдентика

- **Герой бренда — маскот.** Розовый персонаж класса становится проводником: встречает на входе, ведёт по разделам, радуется собранному взносу. Генерируем AI-набор согласованных поз одного персонажа: машет / держит портфель / показывает лапой / спит (для пустых состояний) / салютует конфетти (для успехов). Один персонаж — один характер: добрый, чуть неуклюжий, всегда на стороне родителя.
- **Фирменный знак света.** Диагональный тёплый луч (как из школьного окна) — повторяющийся графический мотив: на логине, в шапках разделов, в пустых состояниях. Узнаваемость строится не на логотипе, а на свете.
- **Значок 1 «Г»** остаётся: круглая эмблема-пуговица, но получает лёгкий объём (внутренняя тень + блик сверху), как эмалированный значок на рюкзаке.
- **Голос интерфейса:** по-человечески, от первого лица маскота, без канцелярита. «Я собрал всё важное здесь», «Каждая копейка на виду», «Завтра — спортивная форма».

## 3. Палитра «Золотой час» (эволюция текущей)

Сохраняем узнаваемые синий/золотой/розовый, но углубляем и добавляем свет.

| Роль | Цвет | HEX |
|---|---|---|
| Фон «Тетрадный лист» | тёплый крем | `#FBF6EC` |
| Фон-глубина (низ страниц) | персиковая дымка | `#F6E8D8` |
| Чернильный (текст, заголовки) | тёплый тёмно-синий | `#2A2B45` |
| Синий бренда | глубокий васильковый | `#4A5BD4` → тень `#37449F` |
| Золото «золотого часа» | градиент | `#FFD98A → #F2A93B` |
| Розовый маскота | живой пион | `#F49AB8`, глубина `#E56E97` |
| Мята (успех, «собрано») | шалфей | `#7FC8A9` |
| Свет-луч | почти белое золото | `#FFF3D6` при 55–70% |

Правила: золото — только для света, денег и праздника (не для текста); чистый белый запрещён — вместо него крем; тени всегда тёплые (`#B98E5E` при 12–18%, никогда не серо-чёрные) — это главный «дорогой» приём палитры.

## 4. Типографика

- **Дисплейная — Baloo 2 (Bold/ExtraBold).** Круглая, добрая, отлично говорит по-русски. Только заголовки h1–h2 и большие цифры кассы. Крупно и смело: h1 на телефоне 34–40px, на десктопе 56–72px, межстрочный 1.05, лёгкий отрицательный трекинг (−1%).
- **Текстовая — Nunito (400/700/800).** Остаётся: подписи, абзацы, кнопки. Размер тела 15–16px, строка 1.55.
- **Деньги — отдельный жанр.** Суммы всегда Baloo 2, tabular-nums, с уменьшенной валютой: `1 373,00 <small>BYN</small>`. Деньги — самый эмоциональный контент приложения, они должны выглядеть как цифры на табло, а не как текст.
- Иерархия: один h1 на экран, надзаголовки капсом 11px с трекингом +8% (как сейчас «СЕЙЧАС В КАССЕ» — это уже хорошо, оставляем).

## 5. Стиль изображений

- **Только рендер и иллюстрация, ноль стоковых фото.** Персонаж и сцены — мягкий 3D-рендер «как из мультфильма»: матовые материалы, подповерхностное свечение (персонаж будто слегка светится изнутри), малая глубина резкости на фонах.
- **AI-набор маскота:** 6–8 поз, единый ракурс камеры 3/4, единый свет слева-сверху, фон прозрачный/крем. Референс-стиль: Pixar-персонаж + мягкость «клеевого» пластилина.
- **Сцены-диорамы** для обложек разделов: миниатюрные наборы (парта и рюкзачок — «Расписание»; копилка и монеты — «Сборы»; тележка — «Покупки»). Как кукольные наборы, снятые макрообъективом.
- Единая обработка: тёплый баланс белого, лёгкое зерно 2–3% поверх рендеров (склеивает всё в «кино»), никакого глянцевого пластика.

## 6. Композиция

- **Сетка с воздухом.** Поля больше обычного: контент ~1080px, поля на телефоне 20px, отступы между секциями 56–72px. Дорого = не тесно.
- **Кадры, а не блоки.** Каждая секция строится как кадр фильма: один смысловой центр, поддержка светом, второстепенное — мельче и тише. Правило «одна секция — одна мысль».
- **Три плана глубины.** Фон (луч, мягкие пятна света) → средний план (карточки) → передний (маскот, стикеры, летящие монетки). Передний план может чуть перекрывать карточки — это создаёт «объём диорамы».
- Скругления укрупняем: карточки 24–28px, кнопки-капсулы полные, у больших обложек 32px.

## 7. Свет — главный спецэффект

- **Ключевой свет:** тёплый, слева-сверху, под ~35°. Все тени и блики в приложении подчиняются одному источнику — это незаметно, но именно это «склеивает картинку» как в кино.
- **Луч в шапках:** мягкий диагональный градиент `#FFF3D6 → прозрачный` + едва заметные пылинки (3–5 частиц, медленный дрейф).
- **Тени двухслойные:** близкая контактная (`0 2px 6px` тёплая 14%) + дальняя рассеянная (`0 18px 44px` тёплая 10%). При наведении дальняя растёт — карточка «взлетает к свету».
- **Блик-каём:** у активных элементов тонкая верхняя кромка света (inset 1px белое золото 40%) — эффект дорогого «стеклянного» объёма без стекломорфизма.

## 8. Фирменные интеракции

1. **Живой маскот.** Глаза и наклон корпуса следят за курсором (на телефоне — лёгкий отклик на гироскоп/тап); моргает раз в 4–7 сек; при долгом бездействии — зевает. Это «сердце» узнаваемости.
2. **Звонкая касса.** Наведение на сумму в кассе — монетка подпрыгивает и падает в цифру, сумма делает микро-«пружину». После подтверждения чека — короткий дождь из 8–10 монет.
3. **Магнитные кнопки.** Главные CTA слегка тянутся к курсору (до 4px) и мягко «выдыхают» при отпускании.
4. **Стикеры-печати.** Статусы («собран», «завтра») появляются как штамп: масштаб 1.4 → 1.0 с лёгким поворотом −4° — будто печать поставили в дневник.
5. **Прогресс как история.** Полосы прогресса заполняются золотым светом слева направо с «искрой» на конце фронта заливки.

---

## 9. Система движения (motion-дизайн)

Философия: **анимация Pixar, дозировка Apple.** Всё движение — с характером (предвкушение, упругость), но короткое и всегда подчинено смыслу. Пользователь не должен ждать анимацию никогда.

### 9.1 Токены движения

| Токен | Значение | Применение |
|---|---|---|
| `--ease-out-soft` | `cubic-bezier(.22,.61,.21,1)` | появления, раскрытия |
| `--ease-spring` | `cubic-bezier(.34,1.45,.5,1)` | маскот, кнопки, штампы (overshoot ~6%) |
| `--ease-exit` | `cubic-bezier(.55,0,.7,.4)` | исчезновения — всегда быстрее появлений |
| `--t-micro` | 160–220 мс | hover, нажатия, переключатели |
| `--t-base` | 380–450 мс | карточки, модалки |
| `--t-scene` | 650–800 мс | смена экранов, hero |
| `--stagger` | 55 мс | каскад в списках (максимум 6 элементов, дальше — вместе) |

### 9.2 Появление секций (scroll-reveal)

- Элементы входят из `opacity 0, translateY 22px, scale .985` → норма, `--t-base`, `--ease-out-soft`.
- Каскад: заголовок секции → карточки слева направо со `--stagger`. Цифры в статистике докручиваются count-up за 600 мс, но только при первом появлении.
- Порог срабатывания: элемент виден на 18%. Анимация — строго один раз за сессию (повторные скроллы ничего не дёргают — важное правило «не отвлекать»).

### 9.3 Parallax

Три слоя, только transform (без layout):
- фон-луч и пятна света: 0.4× скорости скролла;
- декоративный передний план (стикеры, монетки): 1.15×, амплитуда ±14px;
- контент: 1× (никогда не паралlaxим смысл — двигается только декорация).
На курсор: сцена hero наклоняется до 1.5°, слои смещаются ±6/±12px с инерцией (lerp 0.08). На телефонах курсорный параллакс заменяется откликом на наклон устройства (амплитуда вдвое меньше) либо отключается.

### 9.4 Hover и микровзаимодействия

- Карточки: подъём `translateY(-4px)` + рост дальней тени, 200 мс. Внутри ничего не меняет цвет — двигается только «высота».
- Кнопки: нажатие `scale .97` (90 мс) → отпускание с `--ease-spring`; успех — короткая вспышка светлого блика по диагонали.
- Поля ввода: рамка «зажигается» тёплым золотом мягкой волной от места клика.
- Переключение вкладок навигации: активная капсула переезжает к новой вкладке (shared underline), 260 мс.
- Тосты: въезжают снизу с пружинкой, уходят вниз с ускорением; иконка тоста делает один «кивок».

### 9.5 Переходы между экранами

- Вкладки: старый контент — fade + `translateY(-10px)` за 180 мс, новый — reveal-каскад. Общее ощущение «перелистнули страницу тетради».
- Вход в приложение: кремовая «шторка» с лучом света проходит слева направо (650 мс), маскот на долю секунды «пробегает» за ней — фирменный момент, который запоминается.
- Модалки: подложка затемняется тёплым (не чёрным!) `#2A2B45` при 45%, окно поднимается снизу с `--ease-spring`.

### 9.6 Поведение на скролле

- Шапка при скролле вниз сжимается в компактную полосу (лого + вкладки), при скролле вверх возвращается — 240 мс.
- Hero-заголовок медленно тает (opacity до 0 к концу первого экрана) — свет «передаётся» контенту ниже.
- Никакого scroll-hijacking: колесо всегда крутит страницу честно.

### 9.7 Границы дорогого вкуса и производительность

- Одновременно анимируется не больше двух вещей в кадре внимания.
- Анимируем только `transform` и `opacity`; никаких `top/left/width`, `blur` в движении — редко и мелко.
- `prefers-reduced-motion` — всё сводится к быстрым fade 120 мс.
- Бюджет: 60 fps на среднем Android; частицы — max 6; JS-анимации через `requestAnimationFrame` c lerp, без тяжёлых библиотек (достаточно CSS + ~3 КБ своего кода).

---

## 10. Первый экран: концепция «Свет в кабинете 166»

Одна визуальная идея живёт в двух местах: **экран входа** (полная версия) и **шапка главной** (короткое эхо).

### Композиция
Широкий кадр «золотого часа»: из правого верхнего угла в сцену падает мягкий световой луч. В луче, справа, стоит маскот с портфелем — он только что «пришёл в класс». Слева — крупный заголовок и кнопки входа. Внизу едва видна «парта» — полка, на которой после скролла/входа разложатся карточки (касса, расписание). Глубина: луч и пылинки сзади, маскот в среднем плане, пара летящих элементов (бумажный самолётик, монетка) чуть перекрывают заголовок спереди.

### Заголовок (сообщение за 2 секунды)
> **Спокойно. Всё про наш класс — здесь.**
> Касса, расписание и решения 1 «Г» — в одном месте. Больше ничего не потеряется в чатах.

Кнопки: «Я родитель» (золотая, тёплая) и «Я из комитета» (чернильная контурная).

### Изображение
AI-рендер маскота «встречающая» поза: стоит вполоборота, машет, портфель в другой лапе. Мягкий рим-лайт по контуру со стороны луча. Никакого тяжёлого WebGL — послойный 2.5D: PNG-слои + CSS-параллакс (это и есть «живо, но легко»).

### Свет и глубина
Луч `#FFF3D6` 60% → прозрачный, поверх — 5 пылинок с медленным дрейфом. Фон под лучом чуть темнее крема (`#F3E7D3`) — за счёт этого свет читается. Тень маскота длинная, тёплая, тянется влево — она «продаёт» время суток.

### Движение камеры и появление (хореография, ~1.6 с)
1. 0–350 мс — из темноты крема «включается» луч (opacity 0→1), сцена начинает медленный dolly-in: scale 1.05 → 1.0 (длится все 1.6 с, `--ease-out-soft`).
2. 250 мс — маскот поднимается снизу с пружинкой (`--ease-spring`) и один раз машет.
3. 450 мс — заголовок собирается по словам (каждое слово: подъём 14px + fade, stagger 55 мс).
4. 800 мс — подзаголовок и кнопки, кнопки с крошечным overshoot.
5. 1100 мс — пылинки и самолётик начинают жить. Сцена готова; дальше — только идл-жизнь (моргание, дрейф).

### Реакция на курсор
Слои следуют за курсором с инерцией: луч ±5px, маскот наклон ±3° и взгляд за курсором, передние элементы ±12px. Кнопки магнитные. На телефоне — отклик на наклон устройства (вдвое тише) и «взмах» маскота по тапу на него.

### Переход после первого скролла / входа
Свет «передаётся вниз»: заголовок тает, маскот с пружинкой уезжает в правый нижний угол и остаётся маленьким помощником, луч сжимается в тонкую золотую полосу под шапкой, и на «парту» каскадом (stagger 55 мс) влетают карточки кассы, расписания и дел — родитель без паузы оказывается в работе. На экране входа тот же переход играет после нажатия кнопки: шторка света слева направо → дашборд.

### Эхо на главной
Шапка дашборда повторяет мотив: тот же луч (короче), маскот в приветственной позе у «Добрый вечер, Кристина!», те же пылинки. Один мир — от входа до работы.

---

## 11. Порядок внедрения (после утверждения)

1. Токены: палитра, тени, скругления, motion-токены в `globals.css`.
2. Типографика: подключение Baloo 2, иерархия, стиль денег.
3. Генерация набора поз маскота (AI) + обработка.
4. Новый экран входа «Свет в кабинете 166».
5. Hero-эхо и reveal-система на главной.
6. Микровзаимодействия (касса, штампы, кнопки) и переходы вкладок.
7. Прогон на телефоне: fps, вес, reduced-motion.
